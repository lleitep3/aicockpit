# .goosehints
## Project: Python Data Pipeline
- We use `pandas` for data manipulation.
- All functions must have complete Google-style docstrings.
- Use `pytest` for all unit tests.
- Never commit data files (`.csv`, `.parquet`) unless they are in the `tests/fixtures/` directory.
