# Subagent: Background Indexer
When asked to summarize the entire repository, use Goose's subagent capability to avoid flooding the context window.

Trigger:
"Start a subagent to read all .md files in the /docs directory and produce a 1-page summary of the architecture. Return only the summary."
