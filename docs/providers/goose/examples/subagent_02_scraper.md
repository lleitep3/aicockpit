# Subagent: Web Scraper
Trigger:
"Spawn a sub-process to curl the sitemap at https://example.com/sitemap.xml, extract all URLs, and find the ones containing '/blog/'. Write the result to blog_urls.txt."
