# AICockpit Installation Manifest
# This file tracks all installed components and their locations
# Used for proper uninstallation and updates

version: "1.0"
cockpit_version: "0.2.0"
installed_at: "2026-06-20T14:00:00Z"
last_updated: "2026-06-20T14:00:00Z"

# Installed agents
agents: []

# Installed skills
skills: []

# Installed hooks
hooks: []

# Installed modules/commands
modules: []

# Configuration
config:
  # Backup directory for uninstallation
  backup_dir: "~/.cockpit/backups"
  
  # Keep backup after uninstall
  keep_backup: true
  
  # Log installation/uninstallation
  log_operations: true
